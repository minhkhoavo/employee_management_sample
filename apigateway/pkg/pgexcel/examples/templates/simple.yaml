version: "1.0"
name: "Simple Report"
description: "Minimal template example - great starting point"

sheets:
  - name: "Data"
    query: "SELECT * FROM your_table LIMIT 100"
    
    layout:
      freeze_rows: 1
      auto_filter: true
      auto_fit_columns: true
